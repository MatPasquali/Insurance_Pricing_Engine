# Tested on Python 3.14. Versions pinned to the current working environment.
numpy==2.4.4
pandas==3.0.3
scikit-learn==1.8.0
shap==0.52.0
matplotlib==3.10.9

# --- ETL (parquet) + notebooks ---
pyarrow
jupyter
nbconvert
ipykernel

# --- Docs (gera o Word: python docs/gerar_documentacao.py) ---
python-docx==1.2.0

# --- Dados v2/v4 do Kaggle (kagglehub baixa dataset publico SEM token) ---
kagglehub
# kaggle   # alternativa via CLI (precisa de token em ~/.kaggle/kaggle.json)

# --- Demo (v1) ---
streamlit

# --- Later phases ---
# statsmodels   # formal GLM summary tables (coef / std err / p-values)
# lightgbm      # alternative GBM challenger (sklearn HistGradientBoosting also works)
# mlflow        # v3 model registry / experiment tracking
